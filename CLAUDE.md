### Changelog
PRs to `dev` must update `CHANGELOG.md` under `## Unreleased`. CI enforces this. After a release replaces `## Unreleased` with the version number, the first PR to `dev` adds a fresh `## Unreleased` heading.